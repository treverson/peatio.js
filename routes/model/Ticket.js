const Util = require('../util/util');
const TABLE = require('../domain/table.define');
// const Members = TABLE.Members;
const Tickets = TABLE.Tickets;

// Tickets.belongTo(Members);

module.exports = Tickets;